Admin can resolve a request
A logged in Admin can resolve a request.
The request has to have a current status of Approved to be resolved.
    ---
    tags:
      - Admin Endpoint
    summary: Approve Request
    consumes:
      - application/json
    produces:
      - application/json
    parameters:
      - in: header
        name: Authorization
        description: Authorization Token
        type: string
        required: true
      - in: path
        name: request_id
        description: Request ID
        type: integer
        required: true