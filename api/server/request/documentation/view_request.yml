User can view all their requests
A logged in user can view all of their requests.
    ---
    tags:
      - Requests Endpoint
    summary: View requests
    consumes:
      - application/json
    produces:
      - application/json
    parameters:
      - in: header
        name: Authorization
        description: Authorization Token
        type: string
        required: true
      - in: path
        name: request_id
        description: Request ID
        type: integer